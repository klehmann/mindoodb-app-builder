node_modules/
dist/
dist-host/
.wrangler/
.DS_Store
coverage/
*.local
settings.json

# The lockfile is committed. Careful when regenerating it: an install run with
# LOCAL_MINDOODB=1 rewrites the first-party entries as file: paths to sibling tarballs,
# which resolve on nobody else's machine. Check for `file:` before committing one.

# pnpm pack output from sibling first-party checkouts.
*.tgz
